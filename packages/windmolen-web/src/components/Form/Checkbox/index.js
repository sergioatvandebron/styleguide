import React from 'react';
import ToggleBase from '../ToggleBase';

const Checkbox = (props) => {
  return (
    <ToggleBase
      type="checkbox"
      {...props}
    />
  )
};

export default Checkbox;
