---
name: Icon
menu: Elements
route: /Elements/Icon
---

import { Playground, PropsTable } from 'docz';
import Wrapper from '../Wrapper';
import Icon, { spritesheet } from './';

# Icon

## Props
<PropsTable of={Icon} />

## Basic usage
<Playground wrapper={Wrapper}>
  <Icon name="windmill" fontSize="5rem" />
  <Icon name="arrow-right" fontSize="5rem" />
  <Icon name="arrow-left" fontSize="5rem" />
  <Icon name="facebook" fontSize="5rem" />
  <Icon name="twitter" fontSize="5rem" />
  <Icon name="instagram" fontSize="5rem" />
</Playground>

## Available icons
<Playground wrapper={Wrapper}>
  {spritesheet.names.map((name) => (
    <div>
      <Icon name={name} fontSize="40px" /> {name}
    </div>
  ))}
</Playground>
